SECRET_KEY=change-me-to-a-long-random-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:3000

# PostgreSQL (本番のみ設定。空のままだとローカルは SQLite を使用)
PGDATABASE=
PGUSER=
PGPASSWORD=
PGHOST=
PGPORT=

# Redis (本番のみ設定。空のままだと DatabaseCache を使用)
REDIS_URL=

FRONTEND_URL=http://localhost:3000
SITE_ICON_URL=
GEMINI_API_KEY=
